Qué le pedí a la IA:  Le pedí a la IA que, actuando como un mentor de programación Frontend, me ayudara a construir la base del código HTML y JavaScript para gestionar el arreglo de usuarios. Le solicité específicamente que me guiara en cómo estructurar las funciones obligatorias (agregarUsuario(), mostrarUsuarios(), filtrarUsuarios()) y en cómo utilizar document.createElement() para inyectar las filas en la tabla sin usar innerHTML directo, como lo pedía la rúbrica.

Qué mejoras apliqué:  Gracias a las sugerencias de la IA, apliqué mejoras estructurales separando la lógica de validación en su propia función (validarFormulario()). Además, la IA me sugirió usar variables más descriptivas en el método filter y aplicar Bootstrap para cumplir con los requerimientos adicionales de diseño.